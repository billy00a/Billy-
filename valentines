<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
<title>Валентинка</title>

<style>
body {
  margin: 0;
  padding: env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom) env(safe-area-inset-left);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  font-family: -apple-system, BlinkMacSystemFont, Arial, sans-serif;
  background: linear-gradient(135deg, #ff9a9e, #fad0c4);
  text-align: center;
  overflow: hidden;
}

#titlePhoto {
  max-width: 85vw;
  max-height: 40vh;
  border-radius: 18px;
  margin-bottom: 18px;
  box-shadow: 0 8px 20px rgba(0,0,0,0.25);
}

h1 {
  margin-bottom: 20px;
  font-size: 22px;
}

.buttons {
  position: relative;
  width: 100%;
  height: 260px;
}

button {
  padding: 16px 44px;
  font-size: 18px;
  border: none;
  border-radius: 14px;
  cursor: pointer;
  transition: 0.25s ease;
}

#yesBtn {
  background-color: #4CAF50;
  color: white;
}

#noBtn {
  background-color: #f44336;
  color: white;
  position: absolute;
}

#finalScreen {
  display: none;
  position: fixed;
  inset: 0;
  background: linear-gradient(135deg, #ff5f8a, #ff7eb3);
  color: white;
  justify-content: center;
  align-items: center;
  flex-direction: column;
  text-align: center;
  padding: 24px;
}

#finalScreen img {
  max-width: 90vw;
  border-radius: 22px;
  margin-top: 20px;
  box-shadow: 0 12px 28px rgba(0,0,0,0.35);
}
</style>
</head>

<body>

<img id="titlePhoto" src="IMG_2508.jpeg" alt="Фото">

<h1>Настя, ты станешь моей валентинкой?</h1>

<div class="buttons">
  <button id="yesBtn">Да</button>
  <button id="noBtn">Нет</button>
</div>

<div id="finalScreen">
  <h1>теперь ты влипла💍🤍🥹</h1>
  <img src="IMG_3151.jpeg" alt="Финал">
</div>

<script>
let noBtn = document.getElementById("noBtn");
let yesBtn = document.getElementById("yesBtn");

function moveNoButton() {
  let bw = noBtn.offsetWidth;
  let bh = noBtn.offsetHeight;

  let padding = 14;

  let maxX = window.innerWidth - bw - padding;
  let maxY = window.innerHeight - bh - 220;

  let x = Math.random() * maxX;
  let y = Math.random() * maxY;

  noBtn.style.left = x + "px";
  noBtn.style.top = y + "px";
}

// кнопка уезжает ДО клика
noBtn.addEventListener("touchstart", moveNoButton);
noBtn.addEventListener("mouseover", moveNoButton);

// кнопка ДА
yesBtn.addEventListener("click", () => {
  document.getElementById("finalScreen").style.display = "flex";
});
</script>

</body>
</html>
